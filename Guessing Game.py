### Guessing Game ###

guess = "Thanos"

def guessing_game(guess):
  if len(guess) = 1:
    guess_type = "letter"
  else: 
    guess_type = "word"
  if guess_type == "letter"
  
  
###if they have more than 2 guesses left#
